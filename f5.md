# Claude プラグイン／スキル調査レポート

- 調査日: 2026-06-10
- 対象: `claude-plugins/` 配下の 4 プラグイン・5 スキル（比較対象として `codex-plugins/`、`codex-agents/`、`docs/`）
- 調査方法: 全ファイルの精読レビュー（並列サブエージェント 3 系統）＋ Claude Code 公式ドキュメントとの照合
- 主な出典:
  - https://code.claude.com/docs/en/skills.md
  - https://code.claude.com/docs/en/plugins-reference.md
  - https://code.claude.com/docs/en/plugins.md
  - https://code.claude.com/docs/en/sub-agents.md
  - https://agentskills.io/specification （Agent Skills オープン標準）

---

## 1. このプロジェクトの方針（本レポートで崩さない前提）

リポジトリ構成・README・各 SKILL.md から読み取れる設計方針は以下のとおり。提案はすべてこの方針の維持を前提とする。

| 方針 | 根拠（実装箇所） |
|---|---|
| Codex 版と Claude 版を同名プラグインで並行管理し、プラグインを配布単位とする | `claude-plugins/` と `codex-plugins/` の対構成、`codex-plugins/README.md`「`codex-plugins/` 配下を正式な配布単位として扱います」 |
| HTML 生成系はサブエージェントへ委任し、親のコンテキスト汚染を防ぐ | `html-doc/SKILL.md` 2節・5節、`html-plan/SKILL.md` 2節・5節（「親 Claude が直接 Write/Read をしない」） |
| 生成系スキルは「ユーザーが明示的に指示したターンに限って発動」 | `html-doc` / `html-plan` の description および各 SKILL.md 1節 |
| progressive disclosure（詳細は `references/` へ、SKILL.md 本文はコンパクトに） | 全スキルで実践済み（本文 125〜293 行、いずれも公式推奨の 500 行未満） |
| 外部 CDN 非依存・完全オフラインで動く HTML テンプレート | 両 template.html に `<script>`・外部参照なし |
| ドキュメントは日本語、description は「である体」で統一 | rikai-implement を除く全スキルで統一されている |

---

## 2. 公式仕様との照合で確定した事実（今回の検証結果）

改善提案の根拠となるため、公式ドキュメントで裏取りした事実を先に示す。

### 2.1 description の文字数上限は二本立て

| 仕様 | 上限 | 備考 |
|---|---|---|
| Claude Code 固有 | **1,536 文字**（`description` + `when_to_use` の合計、超過分は切り捨て） | `maxSkillDescriptionChars` 設定で変更可。出典: skills.md Frontmatter reference |
| Agent Skills オープン標準（agentskills.io） | **1,024 文字**（`description` 単体。`when_to_use` フィールド自体が存在しない） | 出典: agentskills.io/specification |

→ 「1024 文字」は誤りではないが Claude Code 固有スキルに対しては古い基準。移植性を重視するなら 1024 に収めるのが安全、という整理になる。

### 2.2 `${CLAUDE_PLUGIN_ROOT}` は公式変数（前回中間報告の訂正）

plugins-reference.md（Environment variables）に明記:

> All are substituted inline anywhere they appear in skill content, agent content, hook commands, monitor commands, and MCP or LSP server configs.

- SKILL.md 本文中の `${CLAUDE_PLUGIN_ROOT}` は**インライン文字列置換**で展開される。html-doc / html-plan のパス解決ステップ 1 は有効であり、「リポジトリ独自の慣例」ではない。
- ただしスキル専用には **`${CLAUDE_SKILL_DIR}`**（SKILL.md のあるディレクトリを指す）がより適切。`references/` はスキルディレクトリ内にあるため、`${CLAUDE_SKILL_DIR}/references/template.html` と書けば 3 段階フォールバック自体を簡素化できる。

### 2.3 `disable-model-invocation: true` の確定挙動

skills.md「Control who invokes a skill」より:

| 設定 | ユーザー発動 | Claude 自動発動 | コンテキスト |
|---|---|---|---|
| デフォルト | 可 | 可 | description が常時コンテキストに載る |
| `disable-model-invocation: true` | 可（`/name` のみ） | 不可 | **description はコンテキストに載らない** |

→ 公式は `/commit` や `/deploy` のような「明示発動のみ」のスキルに推奨。ただし **description が載らない＝Claude はスキルの存在を知らない**ため、「HTML でドキュメントを作って」という自然文では発動しなくなる（`/html-doc` と打つ必要がある）。トレードオフがあるため後述 4.4 で扱う。

### 2.4 `context: fork` は本プロジェクトの委任設計には不適合（採用しない根拠）

skills.md「Run skills in a subagent」より:

> The skill content becomes the prompt that drives the subagent. **It won't have access to your conversation history.**

html-doc / html-plan は「会話で合意したプラン・ドキュメント内容」を親がプロンプトに整形してサブエージェントへ渡す設計であり、フォーク先は会話履歴を持てない `context: fork` ではこの内容を受け取れない。**現行の「親が Agent ツールで general-purpose に委任する」設計が正しく、`context: fork` への移行は方針（委任による反復生成）を壊す**。現状維持を推奨。

### 2.5 現行 frontmatter フィールド一覧（claude-skill-improver の知識更新用）

公式がサポートするフィールド: `name` / `description` / `when_to_use` / `argument-hint` / `arguments` / `disable-model-invocation` / `user-invocable` / `allowed-tools` / `disallowed-tools` / `model` / `effort` / `context`(`fork`) / `agent` / `hooks` / `paths` / `shell`。
ほかに `${CLAUDE_SKILL_DIR}` 変数、`skillOverrides` 設定、コンパクション時の再アタッチ仕様（スキルあたり先頭 5,000 トークン・全体 25,000 トークン）が追加されている。

---

## 3. プラグイン別の問題点と修正提案（根拠付き）

### 3.1 agent-design-toolkit

| 優先 | 場所 | 問題 | 根拠 |
|---|---|---|---|
| 高 | `skills/claude-skill-improver/scripts/validate_frontmatter.sh` | Codex 版より検証が大幅に貧弱（name の kebab-case 検証・description 文字数チェック・エラー件数サマリーが欠落）。frontmatter 外の `name:` もマッチする走査バグあり | Codex 版 `codex-skill-improver/scripts/validate_frontmatter.sh` との差分比較。スキルの目的（公式準拠の検証）と実装が乖離 |
| 高 | 同スクリプト | 実行権限なし（`-rw-r--r--`） | `ls -la` で確認。`./scripts/...` 直接実行で失敗する |
| 中 | 同スクリプト :32–38 | `rg`（ripgrep）依存でフォールバックなし。Codex 版は `grep` を使用しており不整合 | `rg` は POSIX 非標準。CI 等で `command not found` → `set -e` で即死 |
| 中 | `claude-skill-improver/SKILL.md` ほか references | 文字数基準が「1024 文字」のままで Claude Code の現行仕様（1536 文字・`when_to_use` 併用）を反映していない | 上記 2.1（skills.md Frontmatter reference） |
| 中 | `claude-skill-improver/SKILL.md:62`、`claude-subagent-improver/SKILL.md:72` | name 規則の説明に「数字も可（a-z, 0-9）」「64 文字以内」が欠落 | agentskills.io/specification の name 規則 |
| 中 | 両 SKILL.md の description | 自身の `references/best-practices.md` が推奨する記述スタイルと不一致（自己矛盾） | best-practices.md の「良い例」と実 frontmatter の比較 |
| 低 | `claude-skill-improver/SKILL.md:64` | `bash scripts/validate_frontmatter.sh` がカレントディレクトリ前提 | `${CLAUDE_SKILL_DIR}/scripts/validate_frontmatter.sh` と書けば解決（2.2 の公式変数） |
| 低 | references 全般 | Codex 版の references（163 行・195 行）に対し Claude 版（52 行・43 行）は内容が薄い | ファイル行数比較（best-practices.md: 52 vs 163、review-checklist.md: 43 vs 195） |

### 3.2 html-doc-renderer / html-plan-renderer

| 優先 | 場所 | 問題 | 根拠 |
|---|---|---|---|
| 中 | `html-plan/SKILL.md:115` | `{{TOC}}` の説明が `#anchor` という汎用例のみ。テンプレートの固定 section id（`#context` `#files` `#must-read` `#steps` `#verification`）が未列挙で、サブエージェントが誤った anchor を生成しうる | template.html の実 id とSKILL.md 指示の突き合わせ |
| 中 | `html-plan/references/template.html:355` | `break-inside: avoid-page` は Chromium 以外で対応が限定的。html-doc 側は `avoid` で不整合 | CSS Fragmentation 仕様のブラウザ実装状況、html-doc template との比較 |
| 低 | `html-plan/SKILL.md:98–190` | プロンプトテンプレート内でコードフェンスが入れ子になり GitHub / VSCode プレビューが崩れる | CommonMark は同一長フェンスの入れ子を許可しない。外側を ```` ```` ```` （4 連）にすれば解消 |
| 低 | `html-doc/references/template.html:779` | nav 内 `<h2>目次</h2>` が章見出し（`<h2 class="chapter-heading">`）と同階層に並び、スクリーンリーダーの見出しナビゲーションを汚染 | nav に `aria-label="目次"` が既にあるため見出しは不要（WAI-ARIA Landmark パターン） |
| 低 | `html-plan/references/template.html:219` | CSS `content` 内の絵文字（👉）はスクリーンリーダーが絵文字名を読み上げる場合がある | CSS 擬似要素は `aria-hidden` を付与できない |
| 低 | 両 SKILL.md 3.4節 | 3 段階のパス解決フォールバックは `${CLAUDE_SKILL_DIR}` で簡素化できる（`references/` はスキルディレクトリ内のため `CLAUDE_PLUGIN_ROOT` より適切） | 2.2（skills.md Available string substitutions） |
| 情報 | 両プラグイン | 発動条件・パス解決・上書きルール・戻り値ルールがほぼ同文で重複。片方だけ修正されるリスク | 両 SKILL.md の同文比較。プラグイン独立性（配布単位の方針）を保つなら「重複は許容し、修正時は両方直す」旨を各 README に注記するのが現実解 |
| 情報 | `html-doc/README.md` | `html-plan/README.md:81` にあるローカルテスト手順（`claude --plugin-dir ...`）が欠落 | 両 README の比較 |

### 3.3 planning-facilitator

| 優先 | 場所 | 問題 | 根拠 |
|---|---|---|---|
| 高 | `rikai-implement/SKILL.md:3` | description が「ですます体」で、他 4 スキル（である体）とリポジトリ内で唯一不統一 | 全 5 スキルの frontmatter 比較（本調査で全件確認済み） |
| 中 | `SKILL.md:90` | `### 要点` 見出しが行 29 のテンプレート内見出しと重複し、構造が紛らわしい | 同一ファイル内の同名見出し |
| 中 | `SKILL.md:44–51` | 質問フィルターに「以前の回答との矛盾チェック」「推定で進める場合の前提宣言」が欠落。Codex 版 fukabori（section 2 条件 5）には存在する | fukabori/SKILL.md との比較 |
| 低 | `README.md:21` | 「そのターンで」という表現がエンドユーザーに伝わりにくい（「そのメッセージで」が明確） | 用語の一般性 |

---

## 4. プロジェクトをさらに良くするための提案（方針維持・根拠付き）

### 4.1 claude-skill-improver を「現行仕様の教科書」に更新する（最優先）

- **何を**: `references/best-practices.md` とチェックリストに、2.5 の現行 frontmatter 一覧、文字数の二本立て基準（Claude Code 1536 / オープン標準 1024）、`${CLAUDE_SKILL_DIR}`、コンパクション再アタッチ仕様（5,000 / 25,000 トークン）を反映する。
- **なぜ最優先か**: このスキルは他の全スキルの品質基準として機能する（description に「SKILL.md を Claude Code 向けに最適化したいとき…に使う」と自認）。基準自体が古いと、今後作るスキルすべてに古い制約が伝播する。
- **方針との整合**: agent-design-toolkit の存在目的（「エージェント設計と skill 定義の改善」、ルート README:7）そのもの。
- **あわせて**: Codex 版 references（163 行・195 行）の充実度に合わせて Claude 版 references を増強する。Codex 版と Claude 版の「対」構成という方針上、品質も対称であるべき。

### 4.2 validate_frontmatter.sh を Codex 版と同水準＋公式準拠に強化する

- **何を**: name の kebab-case 検証（a-z, 0-9, ハイフン・64 文字以内）、description 文字数（1024 警告 / 1536 エラーの二段階が両仕様に対応できて合理的）、frontmatter 内に限定した走査、エラー件数サマリー、`grep` ベース化（`rg` 依存除去）、実行権限付与（`chmod +x`）。
- **根拠**: Codex 版スクリプトが既に同等機能を `grep` で実装済み（実装可能性の証明）。「Codex 版と Claude 版の対称性」という方針にも適合。文字数の二段階は 2.1 の仕様差に基づく。

### 4.3 パス参照を `${CLAUDE_SKILL_DIR}` に統一する

- **何を**: html-doc / html-plan の 3.4 節のパス解決を「第 1 優先: `${CLAUDE_SKILL_DIR}/references/...`、フォールバック: 現行の探索」に変更。claude-skill-improver のスクリプト呼び出しも `${CLAUDE_SKILL_DIR}/scripts/...` に。
- **根拠**: skills.md が「カレントディレクトリに依存せずスキル同梱ファイルを参照する」用途として公式に案内している（2.2）。現行の 3 段階フォールバックより誤動作の余地が小さく、SKILL.md も短くなる（progressive disclosure の方針に合致）。
- **注意**: `${CLAUDE_SKILL_DIR}` は SKILL.md 本文の文字列置換でのみ展開される。サブエージェントへ渡すプロンプトには「展開済みの絶対パス」を埋め込む現行運用を維持すること（委任先では変数が展開されないため）。

### 4.4 html-doc / html-plan の発動制御は「現状維持」か「`disable-model-invocation: true`」かを明示的に選ぶ

- **選択肢 A（現状維持）**: 「HTML でドキュメントを作って」という自然文でも発動できる。代わりに description が常時コンテキストを消費し、誤発動リスクを description の文章で抑止し続ける。
- **選択肢 B（`disable-model-invocation: true` 追加）**: 誤発動・連鎖発動が仕様レベルで不可能になり、description のコンテキスト消費もゼロになる。代わりに発動は `/html-doc` `/html-plan` のスラッシュコマンドに限定され、自然文では発動しなくなる。
- **根拠**: 2.3 の公式仕様（description not in context）。公式は「副作用のある明示発動ワークフロー」に B を推奨しているが、両スキルの description は自然文トリガー（「HTML でドキュメントを作って」等）を明記しており、これを残したいなら A が正しい。**どちらでも方針（明示発動のみ）は守れるため、トレードオフを理解した上での選択を README に記録しておくことを推奨**。
- **B を選ぶ場合の補足**: description は「`/html-doc` で呼び出す前提」の短い説明に書き換えられ、1,536 文字枠の節約になる（2.3 の公式注記）。

### 4.5 `context: fork` は採用しない（判断の記録）

- 2.4 のとおり、フォーク先は会話履歴を持てないため「会話で合意した内容を HTML 化する」両スキルでは機能しない。**現行の Agent ツール委任が公式仕様に照らしても正しい設計**であることが確認できたので、SKILL.md か README に「context: fork を使わない理由」を一行残しておくと、将来の見直しで再調査するコストを防げる。

### 4.6 Claude 版にも repo ローカル marketplace を整備する

- **何を**: ルートに `.claude-plugin/marketplace.json` を追加し、`claude-plugins/` 配下 4 プラグインを登録する。あわせてルート `README.md`（現在 Codex のみ記載、1〜3 行目「Codex 向けの skill と…を管理するリポジトリです」）に Claude 版の一覧とインストール手順を追記する。
- **根拠**:
  - 現状、Claude 版のインストール導線は `html-plan/README.md` の `claude --plugin-dir` 例のみで、Codex 版（`.agents/plugins/marketplace.json` による marketplace 配布、ルート README に手順明記）と非対称。
  - Claude Code は marketplace によるプラグイン配布を公式サポートしている（plugins.md / plugins-reference.md）。
  - 「プラグインを配布単位とし、Codex/Claude を対で管理する」という本プロジェクトの方針に対して、配布手段だけが Codex 片側にしか無いのは方針の未完了部分と言える。

### 4.7 Codex 版にしか無いプラグイン／スキルの計画的な移植

方針が「同名プラグインの対管理」である以上、以下は対称性のギャップ。優先度順に:

| 対象 | 移植価値 | 根拠 |
|---|---|---|
| `git-ops-helper`（git-push-branch） | 高 | 39 行の小スキルで移植コストが低い。秘匿ファイルガード・保護ブランチ警告・PR リンク提示は Claude Code でも同様に有用（Claude Code の git 操作はスキル化しなくても可能だが、運用ルールの標準化が目的） |
| `ui-craft`（ui-delight） | 高 | references 3 ファイル含め 213 行。プラットフォーム非依存の内容（デザイン規律）でほぼそのまま移植可能 |
| `planning-facilitator` への `fukabori` 追加 | 中 | rikai-implement（実装ゲート）と fukabori（要件深掘り）は補完関係。両方あって「計画→実装」の一貫ワークフローが完成する。Codex 版で内容が実証済み |
| `agents-md-improver` → `claude-md-improver` 化 | 低 | CLAUDE.md 改善は既存の外部スキル（claude-md-management）で代替可能。重複投資になるため見送りが妥当 |

### 4.8 plugin.json のメタデータ拡充（小粒・任意）

- **何を**: 4 プラグインに `keywords` と `displayName`（v2.1.143+ で公式サポート）を追加。
- **根拠**: plugins-reference.md のスキーマに正式フィールドとして存在し、Codex 版 plugin.json は `keywords` 等を既に持つ（対称性）。marketplace 整備（4.6）とセットで検索性・UI 表示が改善する。

---

## 5. 推奨対応順

1. **claude-skill-improver の知識更新＋validate_frontmatter.sh 強化**（4.1, 4.2）— 他スキルの品質基準が先
2. **既存スキルの個別修正**（3.1〜3.3 の高・中: 文体統一、name 規則記述、`{{TOC}}` 固定 id、`break-inside` 統一、矛盾チェック追加）
3. **`${CLAUDE_SKILL_DIR}` への統一**（4.3）と **発動制御の選択を記録**（4.4, 4.5）
4. **Claude 版 marketplace ＋ルート README 更新**（4.6, 4.8）
5. **git-ops-helper / ui-craft / fukabori の移植**（4.7）— 新規追加なので最後

各項目は独立しており、1 ブランチ 1 テーマで段階的に PR 化できる。
